package bonus.application.shapes;

import java.awt.*;

public class Triangle extends RegularPolygon {

    public Triangle(int x0, int y0, int radius, Color polygonColor) {
        super(x0, y0, radius, 3, polygonColor);
    }
}
